[Unit]
Description=Restore ipsets at boot
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/sh -c '/sbin/ipset restore < /etc/ipset.conf'
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
