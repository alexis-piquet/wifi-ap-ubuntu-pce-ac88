[Unit]
Description=Restore ipsets at boot
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c "ipset destroy allow_all || true; ipset destroy whitelist || true; /sbin/ipset restore -f /etc/ipset.conf"
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
